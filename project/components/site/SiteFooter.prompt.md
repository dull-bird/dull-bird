The site footer: bird mark and the William Gibson line the brand is built on, a few nav columns, and an "for agents" link to the machine-readable view.

```jsx
<SiteFooter lang="zh" agentHref="/llms.txt" />
<SiteFooter lang="en" quote="Slow bird, early start." />
```

Pass `columns` to override the default Site / Follow / Elsewhere groups. The Gibson quote is the brand's north star — keep it unless localizing.
